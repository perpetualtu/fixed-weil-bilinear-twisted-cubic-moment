\section{Proof ledger and reproducibility}
\label{app:audit}

This appendix records the dependency structure used to check the proof.  It
is not a substitute for the arguments in the body of the paper.

\subsection{Dependency ledger}

\begin{center}
\small
\begin{tabular}{>{\raggedright\arraybackslash}p{0.06\textwidth}
                >{\raggedright\arraybackslash}p{0.61\textwidth}
                >{\raggedright\arraybackslash}p{0.21\textwidth}}
\toprule
ID & claim & location\\
\midrule
A1 & The unnormalized AFE includes all four remote completed-zeta residues,
and the long zeta series is expanded only on $\Re u=1$. &
\Cref{lem:afe}, \eqref{eq:zeta-star}\\
A2 & The coupled weight has simultaneous product decay on complete vertical
contours in all four size quadrants. & \Cref{lem:double-weight}\\
A3 & Extracting $d=(h,km)$ gives a coprime inverse and exact Poisson density
$d/h$. & \eqref{eq:gcd-decomposition}--\eqref{eq:poisson-density}\\
A4 & The fixed-$l$ nonzero frequencies are
$O(T^\eps KH^{3/2})$, with the actual $TS$ Poisson-integral size retained. &
\Cref{prop:block-weil,prop:nonzero-global}\\
A5 & The artificial $l<1$ and $n<1$ endpoint regions have arbitrary saving
on the good half-space; the singular layer has exact gcd cancellation. &
\Cref{prop:endpoint-deletion}, \eqref{eq:d-cancellation}\\
A6 & All four branches reach genuine Tonelli domains.  Full contour heights
are restored before finite and Abel regulators are removed. &
\eqref{eq:lambda-corridor}--\eqref{eq:dual-tonelli}\\
A7 & The beta integrals, finite Euler product, and archimedean reflection
identities give the evaluated zero modes. & \Cref{sec:beta}\\
A8 & Three pairs of fixed-$u$ small $s$-residues cancel before the diagonal
boundary is separated. & \eqref{eq:first-common-Z}--\eqref{eq:third-common-Z}\\
A9 & Quantitative continuation is applied only to
$\mathscr A_T-\mathscr R_T-\mathscr D_T$. &
\Cref{prop:full-kernel-continuation}\\
A10 & The diagonal-boundary and permutation $u$-moves retain only their
$u=0$ residues; $-1,\ldots,-M$ are killed by $R_M$. & \Cref{sec:final}\\
A11 & Double and triple shift collisions are jointly removable by local
division by three linear factors. &
\Cref{lem:three-divisor,prop:P-holomorphic}\\
A12 & Combining the zero mode, nonzero mode and diagonal boundary gives the
error stated in \Cref{thm:main}. & final paragraph of \Cref{sec:final}\\
\bottomrule
\end{tabular}
\end{center}

\subsection{Additional adversarial checks}

We record eight stress tests applied to the quantitative and uniformity
claims used in the proof.

\begin{enumerate}[label=\textup{D\arabic*.},leftmargin=10mm]
\item
The coefficient hypotheses are used through the actual block norms
$\mathcal A_{H_0}$ and $\mathcal B_{K_0}$ in
\eqref{eq:coefficient-block-norms}, or through the explicit outer absolute
sum in \eqref{eq:majorant-hk}.  After choosing a smaller auxiliary exponent,
\[
 \sum_{h\leq H}\sum_{k\leq K}
 \frac{|a_hb_k|}{\sqrt{hk}}
 \ll_\eps T^\eps(HK)^{1/2}.
\]
No $\ell^1$ coefficient norm is replaced by a supremum without its associated
counting factor.

\item
Every occurrence of $T^\eps$ is interpreted after choosing the auxiliary
divisor and localization exponents sufficiently smaller than the final
$\eps$.  In particular, \eqref{eq:completed-weight-derivatives} is obtained
only after the fixed completion order is known.  The logarithmic dyadic
counts and the finitely many subsequent losses may therefore be combined
into one final $T^\eps$.  The coarser absorption used to prove the polynomial
majorant in \Cref{prop:full-kernel-continuation} is not used as part of the
sharp error term.

\item
The only size consequences used beyond \eqref{eq:main-range} are
\[
 HK\leq T^{1-\eta},\qquad
 H\leq T^{2(1-\eta)/3},\qquad
 K\leq T^{1-\eta},
\]
which follow from $H,K\geq1$.  These are sufficient for
\eqref{eq:scale-contradiction}, \eqref{eq:n-endpoint-saving}, the polynomial
majorants, and the two final left-line estimates.  No ordering between $H$
and $K$, or stronger hidden length condition, is used.

\item
All differentiation and integration-by-parts orders are fixed before $T$.
Finite completion uses only a fixed number of derivatives, while the endpoint
orders depend on the prescribed fixed saving.  The parameter order
\[
 (B_0,C_0,D)\ \longrightarrow\ M\ \longrightarrow\ N\
 \longrightarrow\ (J,P,Q)
\]
is recorded in \eqref{eq:M-before-N}--\eqref{eq:N-after-M}.  Consequently
only finitely many seminorms from \eqref{eq:w-derivatives} enter the implied
constants.

\item
After scaling the shifts by $\log T$, the domains
\eqref{eq:majorant-domain} and their enlargement have a fixed collar, and
the contours in \eqref{eq:majorant-contours} are independent of $T$ and of
the point in the target compact set.  The threshold
\eqref{eq:majorant-T-threshold} keeps the kernel divisors and the relevant
moving poles a fixed distance from these contours.  Thus no continuation
argument relies on a collar shrinking with the shifts.

\item
Uniformity is checked simultaneously at
\[
 \alpha+\gamma=0,\qquad
 \beta+\gamma=0,\qquad
 \alpha-\beta=0,
\]
including the common locus $\alpha=\beta=-\gamma$.
The local coordinates in \eqref{eq:P-collision-coordinates} satisfy
$z=x+y$, and \Cref{lem:three-divisor} proves divisibility by the full product
$xyz$, rather than cancellation only at generic points.  Fixed-radius Cauchy
estimates on the scaled domain preserve the stated uniform bounds at the
filled-in values.

\item
Individual meromorphic terms are not estimated on a collision divisor and
then combined formally.  The complete permutation sum is retained before
\eqref{eq:P-polynomial} is applied; the direct diagonal, reflected dual
diagonal, and zero mode are assembled before the boundary estimate
\eqref{eq:D-T-Z-bound}; and the source object is bounded from its exact-cutoff
representation.  Cancellation therefore precedes every absolute-value
estimate that crosses a collision.

\item
The final error ledger contains both surviving contributions:
\eqref{eq:nonzero-final} gives
$T^\eps KH^{3/2}$, and \eqref{eq:boundary-zero-bound} gives
$T^{1/2+\eps}(HK)^{1/2}$.  The AFE recovery error, the continued endpoint
error, and both regulator left lines are smaller than any prescribed fixed
power by \eqref{eq:full-continuation-result},
\eqref{eq:boundary-move-result}, and \eqref{eq:permutation-new-line}.
Thus the terms retained in \eqref{eq:main-asymptotic} cover the complete
error decomposition.
\end{enumerate}

\emph{Scope of the audit.}
No first unsupported inference was found in these eight internal checks.
This is a line-by-line consistency audit, not a formal verification or an
external peer-review certificate.  The finite numerical tests likewise
check only explicit identities.  Independent expert review remains
necessary, especially for the regulator--Tonelli bridge and the assembled
continuation and residue arguments.

\subsection{Finite identity checks}

The accompanying script \path{verification/check_zero_identities.py}
uses arbitrary-precision arithmetic to test the two gamma identities and the
linear approach to two collision divisors.  A reference run at 80 decimal
digits produced relative errors between approximately $10^{-52}$ and
$10^{-80}$ for the gamma identities.  In the collision tests, decreasing the
displacement from $10^{-12}$ to $10^{-16}$ to $10^{-20}$ decreased the
successive differences by a factor of $10^{-4}$ at each step.  These are
finite consistency checks only; the contour deformations, Tonelli arguments,
and continuation are proved analytically in the body of the paper.

\subsection{Release integrity}

The source archive distributed with this paper contains the complete
manuscript source, bibliography, compiled PDF, numerical verification script,
and its fixed-version arbitrary-precision dependency and license.  The file
\path{MANIFEST.sha256} records a SHA256 digest for every other payload file in
the archive.  The verification script uses explicit release thresholds and
prints \texttt{status=PASS} only after all checks have completed.
